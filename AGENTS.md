## Learned User Preferences
- Prefer ARM64 deliverables to be as self-contained as possible, and explicitly question regressions from single-file packaging.

## Learned Workspace Facts
- ARM64 packaging and deployment are actively done via `docker-build.bat` and tested on Raspberry Pi using the packaged `run.sh`.
